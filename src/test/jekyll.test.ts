import {convertToJekyll} from "../jekyll"

describe("jekyll", () => {
    // convertToJekyll()

    test('1 + 1 = 2', () => {
        expect(1 + 1).toBe(2)
    })

    it.todo("should read a file in ready directory only")

    it.todo("![NOTE] title & contents should be converted to content {: .prompt-info}")
})

describe("ready directory 안에 .md 파일을 읽을 수 있는지 테스트", () => {

})
